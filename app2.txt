hello git.

git branch -M main // Master branch Name Change to main

git add app2.txt
git commit  -m 'commit message'

s

git add .                 // add (staging)
git commit -m 'commitMsg' // commit

git branch (branchName) // branch create
git switch (branchName) // branch open
git status // branch check

git merge (branchName)

git branch -d (branchName) // merge completed brnach delete
git branch -D (branchName) // not merge branch delete

asdasd